<% if (blogs.length > 0) { %>
    
<h4 class="mt-2"><span class="badge badge-info">آخرین پست ها</span></h4>

<div class="table-responsive">
    <table class="table table-hover ">
        <thead>
            <tr>
                <th>#</th>
                <th>عنوان</th>
                <th>تاریخ</th>
                <th>وضعیت</th>
                <th>ویرایش</th>
                <th>حذف</th>
            </tr>
        </thead>
        <tbody>
            <% for (let blog of blogs) { %>
            <tr>
                <td>1</td>
                <td><a href="/blogs/<%= blog._id %>"></a><%= blog.title %></td>
                <td><%= formatDate(blog.createdAt) %></td>
                <td>
                    <span class="badge <%= blog.status === "public" ? 'badge-success' : 'badge-warning' %>">
                        <%= blog.status === "public" ? "عمومی" : "خصوصی" %>
                    </span>

                </td>
               <td>
                    <a href="/dashboard/edit-post/<%= blog._id %>" class="btn btn-sm btn-info">ویرایش</a>
                </td>
                <td>
                    <a  href="/dashboard/delete-post/<%= blog._id %>" class="btn btn-sm btn-danger">حذف</a>
                </td>
            </tr>
            <% } %>
        </tbody>
    </table>

    <%- include("../includes/pagination.ejs") %>
</div>
<% } else { %>
<div class="text-center">
    <h3 class="mt-2">
        در حال حاضر هیچ پستی توسط شما در پایگاه داده ثبت نشده است
    </h3>
</div>
<% } %>
